require "spec_helper"

describe DivineRapier do
  it "has a version number" do
    expect(DivineRapier::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
